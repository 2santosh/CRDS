package Dashboard;
/*
import java.awt.event.ActionEvent;
import java.awt.event.ActionListener;
import javax.swing.JButton;
import javax.swing.JPanel;
import javax.swing.SwingUtilities;

public class AddPoliceStationForm {
	    private static void createMenuItem(JPanel menuPanel, String itemName, String... subItems) {
	        JButton menuItem = new JButton(itemName);
	        // ... (other code)

	        menuItem.addActionListener(new ActionListener() {
	            public void actionPerformed(ActionEvent e) {
	                if (currentSubNavPanel != null && currentSubNavPanel != subNavPanel) {
	                    currentSubNavPanel.setVisible(false);
	                }

	                if (itemName.equals("Police Station")) {
	                    if (currentSubNavPanel == subNavPanel && currentSubNavPanel.isVisible()) {
	                        toggleSubNav(subNavPanel);
	                        currentSubNavPanel = null;
	                    } else {
	                        currentSubNavPanel = subNavPanel;
	                        AddPoliceStationPanel policeStationForm = new AddPoliceStationPanel();
	                        centerPanel.removeAll();
	                        centerPanel.add(policeStationForm);
	                        centerPanel.revalidate();
	                        centerPanel.repaint();
	                        toggleSubNav(subNavPanel);
	                    }
	                } else {
	                    toggleSubNav(subNavPanel);
	                }
	            }

				@Override
				public void actionPerformed(ActionEvent e) {
					// TODO Auto-generated method stub
					
				}

				@Override
				public void actionPerformed(ActionEvent e) {
					// TODO Auto-generated method stub
					
				}
	        });
	        // ... (other code)
	    }

	    // ... (other methods)

	    public static void main(String[] args) {
	        // This is the entry point of your program
	        SwingUtilities.invokeLater(new Runnable() {
	            public void run() {
	                createAndShowMainFrame();
	            }
	        });
	    }
	}

}
*/
